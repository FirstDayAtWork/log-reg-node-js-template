1. Install :
    express - server,
    ejs - template engine,
    nodemon - auto refresh server thing // npx nodemon <app.js>
    express-validator,
    .env,
    jsonwebtoken,
    cookieparser,
    sqlite3 as db.
    bcrypt
2. 